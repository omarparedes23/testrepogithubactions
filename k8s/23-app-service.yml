apiVersion: v1
kind: Service
metadata:
  name: gestiontareas
  namespace: gestiontareas-dev
spec:
  type: NodePort
  selector:
    app: gestiontareas
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
